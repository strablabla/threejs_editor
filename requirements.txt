# Versions testées et compatibles avec le client socket.io 1.3.5 embarqué
# dans les templates, sur Python 3.8.
#
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#   python run.py
#
# Note : Flask-SocketIO 4.x impose des versions anciennes de Flask/Werkzeug
# (run_with_reloader a été retiré de Werkzeug >= 2.1), d'où les pins ci-dessous.

Flask==1.1.4
Werkzeug==1.0.1
Jinja2==2.11.3
MarkupSafe==1.1.1
itsdangerous==1.1.0
click==7.1.2

Flask-SocketIO==4.3.2
python-socketio==4.6.1
python-engineio==3.14.2
eventlet==0.30.2
dnspython==1.16.0

pyserial==3.5
