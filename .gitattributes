# Third-party code: excluded from GitHub's language stats and collapsed in diffs,
# so the repository shows the code actually written here. See static/vendor/VENDOR.md.
static/vendor/** linguist-vendored
example/**       linguist-vendored
